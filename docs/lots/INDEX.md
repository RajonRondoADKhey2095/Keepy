# Index des lots Keepy

> Créé par le **LOT H** (2 septembre 2026), qui a découpé `CLAUDE.md`
> (26 321 lignes de contenu, 133 sections H2) en un fichier de doctrine et
> vingt-cinq fichiers de chantier. **C'est un déplacement, pas une purge** :
> chaque section vit ici *verbatim*, dans son ordre chronologique d'origine.
> Rien n'a été résumé, condensé, réécrit ni supprimé.
>
> La **doctrine permanente** — pièges d'outillage, pièges Godot, doctrine de
> conception, règles d'art, paliers de déploiement — vit dans
> [`CLAUDE.md`](../../CLAUDE.md) et **nulle part ailleurs**.

## Comment lire ce tableau

* **Sections** : nombre de sections H2 d'origine dans le fichier (le
  découpage était *fence-aware* — les `##` à l'intérieur d'un bloc de code
  GDScript sont des commentaires de documentation, pas des titres).
* **Dernier déploiement consigné** : la dernière cérémonie de déploiement
  *écrite dans ce fichier* — `main`, `staging`, ou `—` quand le chantier
  n'en contient aucune. ⚠️ **`—` ne veut PAS dire « pas en production »** :
  les premiers chantiers précèdent l'établissement de la vérification
  sur le service, et un chantier peut avoir été emporté en prod par le
  merge d'un autre.

## Chantiers

| # | Chantier | Fichier | Sections | Lignes | Période | Dernier déploiement consigné |
|---|---|---|---|---|---|---|
| CH01 | Pipeline assets Meshy — les six hazards et leurs recolorisations | [CH01_MESHY.md](CH01_MESHY.md) | 14 | 2132 | 11 → 13 août 2026 | `main` |
| CH02 | Palette marécage — direction artistique permanente et `SwampPalette` | [CH02_PALETTE.md](CH02_PALETTE.md) | 3 | 541 | 11 → 23 août 2026 | `main` |
| CH03 | Sondes — budget temps, watchdog, `ProbeTimeoutAudit` | [CH03_SONDES.md](CH03_SONDES.md) | 1 | 109 | 9 août 2026 | — |
| CH04 | Keepy Chased — décor procédural, modèle de mort, poursuivant, audio | [CH04_CHASED.md](CH04_CHASED.md) | 5 | 411 | 9 → 10 août 2026 | — |
| CH05 | Déploiement — paliers staging/main, CI, API périmées | [CH05_DEPLOIEMENT.md](CH05_DEPLOIEMENT.md) | 3 | 149 | 8 → 17 août 2026 | — |
| CH06 | Écrans 2D — titre, logo, icône PWA, safe-area, letterbox | [CH06_UI_ECRANS.md](CH06_UI_ECRANS.md) | 7 | 950 | 14 → 19 août 2026 | `main` |
| CH07 | Google Sign-In — proxy `/__/auth/*`, COOP/COEP, rafraîchissement du token | [CH07_AUTH.md](CH07_AUTH.md) | 3 | 678 | 17 → 18 août 2026 | — |
| CH08 | Firestore — rules versionnées, durcissement auth, plan Firebase | [CH08_FIRESTORE.md](CH08_FIRESTORE.md) | 6 | 1153 | 18 → 22 août 2026 | `main` |
| CH09 | Keepy Quizz — autoload CRUD et premier écran | [CH09_QUIZZ.md](CH09_QUIZZ.md) | 2 | 440 | 18 août 2026 | — |
| CH10 | Keepy Battle — lots 1 à 12 | [CH10_BATTLE.md](CH10_BATTLE.md) | 13 | 3185 | 20 → 22 août 2026 | `staging` |
| CH11 | Hub — du menu 2D au plateau 3D, décor, extensions, MultiMesh | [CH11_HUB_PLATEAU.md](CH11_HUB_PLATEAU.md) | 10 | 2149 | 18 → 25 août 2026 | `staging` |
| CH12 | Eau — géométrie des cinq corps, lake, stream, spawn-lake | [CH12_EAU_GEOMETRIE.md](CH12_EAU_GEOMETRIE.md) | 5 | 1431 | 25 → 26 août 2026 | `staging` |
| CH13 | Eau — rendu : teinte de Keepy, ligne de flottaison, impact | [CH13_EAU_RENDU.md](CH13_EAU_RENDU.md) | 4 | 1014 | 27 août 2026 | `staging` |
| CH14 | Bateau — le ruisseau devient ridable | [CH14_BATEAU.md](CH14_BATEAU.md) | 2 | 500 | 26 août 2026 | `main` |
| CH15 | Plongeoir — la chaîne complète et sa généralisation | [CH15_PLONGEOIR.md](CH15_PLONGEOIR.md) | 2 | 270 | 27 août 2026 | — |
| CH16 | Tourniquet, balançoire et lobe nord | [CH16_TOURNIQUET_BALANCOIRE.md](CH16_TOURNIQUET_BALANCOIRE.md) | 4 | 1243 | 28 août 2026 | `main` |
| CH17 | Hibou — prop statique et vol en boucle | [CH17_HIBOU.md](CH17_HIBOU.md) | 3 | 949 | 28 août 2026 | `main` |
| CH18 | Cabane et navigation multi-niveaux | [CH18_CABANE_NAV.md](CH18_CABANE_NAV.md) | 13 | 3026 | 28 → 31 août 2026 | `main` |
| CH19 | Pie, baiser et hotspot du lit | [CH19_PIE.md](CH19_PIE.md) | 11 | 2244 | 31 août → 1 sept 2026 | `main` |
| CH20 | Ours — lots A à F, du rig animé au siège de balançoire | [CH20_OURS.md](CH20_OURS.md) | 7 | 1256 | 1 → 2 sept 2026 | `staging` |
| CH21 | Tyrolienne — recon, palier 1 (structure/câble), palier 2 (blaireau/porte/nacelle), retouche échelle blaireau + diagnostic point nord, station debout dégagée de l'escalier, tier 3 (trajet solo sur la structure + retour automatique du blaireau au sud) | [CH21_TYROLIENNE.md](CH21_TYROLIENNE.md) | 17 | 2338 | 3 → 4 sept 2026 | `staging` |
| CH22 | Audit visuel du hub 3D — recon pure, lecture seule : inventaire mesuré des 154 nœuds de dessin, budget 61 161 triangles, ancrage, densité XZ, palette (20 albédos sur 25 en bande morte), liste A (7 corrections sans asset) et liste B (5 fiches Meshy), arbitrage des deux décimateurs ; puis lot batch appliquant A1/A2/A3/A6 (61 161 → 60 201 triangles, `Rock` 1,43:1 → 3,14:1) et mesurant la **pire frame** laissée ouverte par la recon — 48 012 primitives, sous le plafond de frame de 50 000 | [CH22_HUB_VISUEL.md](CH22_HUB_VISUEL.md) | 2 | 1147 | 4 sept 2026 | `staging` |
| CH23 | Feu de camp — **recon VFX**, aucune implémentation finale : viabilité de `GPUParticles3D` sous `gl_compatibility` prouvée (2 078 px contre 0 sur frame vide et 980 pour le contrôle CPU), glow mesuré disponible (1 809 px de halo à albédo 1,0, 5 043 à 4,0) mais coûtant une passe plein écran que le hub n'a pas, émission sur unlit re-mesurée **inerte** (0,5451 des deux côtés), dégagement de 3,521 u autour de (19,9 ; 25,4) par enveloppe convexe des huit coins transformés de 452 pièces, coût flamme seule **+56 / +56 / +2** primitives et **+0 à la pire frame par frustum culling** — trois candidats étiquetés livrés sur `staging` pour arbitrage device | [CH23_FEU_VFX.md](CH23_FEU_VFX.md)  ; **lot 2 — comparatif de style** : `GPUParticles3D` confirmé fonctionnel **sur device** par Mathieu (acquis projet, ne plus re-prouver), les trois candidats A/B/C **écartés** pour cause de style et retirés du fichier, remplacés par `D1` / `D2` procéduraux quantifiés en 4 paliers **mesurés sur `campfire_flame.png`** et `E` billboard portant ce même PNG — quantification chiffrée à **6-8 couleurs distinctes contre 1 626-2 050** (facteur ~250), coût ALU **+1,058 / +1,248 / +1,622 ms par 262 144 fragments** mais **non séparable** (plancher de bruit 0,712 ms) et **~0,015 ms/frame réels** une fois multiplié par la couverture, `E` **ne peut pas pixeliser** (5,19-6,50 texels/px, `HubCamera.OFFSET` constante à 11,703 u) ; **lot 3 — objet définitif** : `D1`/`D2` retirés pour de bon, candidat `E` shippé dans `HubCampfire.gd` (remplace `HubFlameRecon.gd`, supprimé) avec six bûches en géométrie réelle (jamais un billboard) en teepee, matériau des troncs réutilisé ; coupe basse vérifiée **0 fuite sur 24 vues** (8 azimuts × 3 distances, masque d'isolement) ; deux échelles posées (x1,6 site recommandé, x1,2 alt à 3 u), clearance bûches comprises **2,815 u / 2,066 u** ; troisième baseline de primitives mesurée (**34 674**, spread intra-process 0, cause cross-process toujours non prouvée) et coût des bûches **+544** primitives (marge 14 782 / 50 000) ; **lot 4 — imbrication** : bûcher bas et étalé (8 rondins, anneau 0,42, apex 0,22) plus `flame_sink` 0,08, la flamme ne lit plus « plantée derrière » ; emprise du feu **0,772 u**, clearance **2,372 u** au site ; **lot 5 — revert de couleur** : `LOG_COLOUR` rendue à `HubBuilder.TRUNK_COLOR`, variante ALT et `Label3D` retirés, recette d'imbrication rejouée (**0 fuite / 24 vues**, contrôle cassé à 127) ; **lot 6 — cercle de pierres** : recon prouvant que les 48 rochers du hub sont **un `MultiMesh` partagé** et qu'on **ne peut pas** y ajouter d'instance (48 → 49 efface **0/48** transforms), d'où un `MultiMesh` propre à l'anneau réutilisant `HubBuilder.rock_mesh()` et `ROCK_COLOR` ; deux variantes d'irrégularité (MARQUE ×1,57 en taille / 29,8-59,8° d'écart, SOBRE ×1,07 / 39,9-51,1°) à **rayon strictement constant (dispersion 0,000000)**, à 3,40 u l'une de l'autre au même `z` ; recette **48 vues, 0 occultation, 0 px de bande basse** (blind check à 25 843 px) ; **MARQUE recommandée**, biais déclaré (SOBRE au slot le plus contraint, 2,258 u contre 3,521 u) | [CH23_FEU_VFX.md](CH23_FEU_VFX.md) | 6 | 1505 | 4 sept 2026 | `staging` |
| CH24 | Feu de camp interactif — **recon pure** (stub, `staging` `23000f1`) : canal `tapped_campfire` proposé sur le patron OWL/CABIN, point d'arrivée à main levée (20,90 ; 23,44) non vérifié au-delà de son extrémité ; puis **LOT 1 — implémentation** : canal livré, aller-retour du blaireau (patron bateau, retrait actif de `tapped_zipline_badger` via `ZiplineDoor.set_badger_at`), et le point de la recon **rejoué sur le segment complet** — défaut trouvé (1,409 u de l'anneau de pierres au retour du bout éloigné de la tyrolienne, bord mesuré 1,529 u) et corrigé par l'axe latéral de la tour, vérifié à 2,189 u des deux bouts par balayage 3600 pas ; aucun binaire Godot dans ce sandbox à aucun des deux lots, donc aucun rendu offscreen ni sonde rouge-avant-vert exécutable — NEXT STEPS CI/device | [CH24_FEU_INTERACTIF.md](CH24_FEU_INTERACTIF.md) | 12 | 222 | 4 sept 2026 | — |
| CH25 | L'ours rejoint le blaireau au feu — **recon pure** (stub) : l'ours EST déjà un `HubActorWalker` (`walk_to`/`turn_to` réutilisables tels quels, mêmes clips `Walking`/`Running`, aucun écart animation) ; **pas de canal de tap à gater** contrairement au blaireau — la balançoire est déclenchée par atterrissage, `_on_seesaw_mounted()` détourne l'ours SANS garde aujourd'hui, aucun gate générique réutilisable dans ce dépôt (chaque prop a son implémentation ad hoc) ; relèvement direct `BEAR_REST → SITE` **écarté** (0,385 u d'un arbre sur le segment complet), balayage 3600 pas trouve une bande valide 45,7°-130,2°, candidat proposé (20,818 ; 27,387) ; écart de face au retour mesuré **-59,76° à -65,21°** vs face de repos canonique (plus du double du cas "quasi imperceptible" 30,6° du blaireau), correctif probable mais non tranché ; aucun binaire Godot dans ce sandbox — NEXT STEPS lot d'implémentation ; puis **LOT 1 — implémentation** : recon reprouvée indépendamment par un second script (rejoué sur les 213 props sans boîte, mêmes conclusions, marges reconciliées -- la recon mesurait centre-à-segment, ce lot mesure surface-à-segment), même candidat (20,818 ; 27,387) retrouvé par un second balayage indépendant ; `BEAR_CAMPFIRE_WALK_RATE` **calculé** (2,9768, dérive résiduelle 0,0001 s, gaté par `assert()`) plutôt que choisi, ce qui a débusqué un glissement de pieds de PRINCIPE dans `HubActorWalker` (rate lu une fois à `_ready()`, jamais retenu si changé plus tard) corrigé À LA RACINE (`walk_to()` réapplique `speed_scale` à chaque appel, no-op pour tout appelant existant) plutôt que contourné ; gate balançoire écrit (patron bateau), état de l'ours symétrique du blaireau, synchronisation des deux acteurs par un état PARTAGÉ unique (`_campfire_guests`, un retap n'est accepté qu'une fois les DEUX invités arrivés) câblée en DUPLIQUANT le patron entre les deux acteurs plutôt qu'en le factorisant (divergences non triviales à chaque étape) ; `turn_to()`/`face()` au retour sur le patron CH24 déjà générique ; toujours aucun binaire Godot, calcul analytique seul, blind-check appliqué à l'outil de mesure lui-même (vérifié capable de faire échouer le candidat rejeté avant d'être cru sur celui retenu) — NEXT STEPS test device obligatoire avant merge `main` | [CH25_OURS_FEU.md](CH25_OURS_FEU.md) | 9 | 407 | 4 sept 2026 | — |
| CH26 | Le monde cozy — **cinq sessions carte blanche** (v1 → v5, 4-5 sept 2026) promues en un lot : direction VOIE A (`CozyPalette`, `CozyScatter`, ~120 GLB), météo vivante à quatre états, trois zones à la MÊME altitude (plateau, Vallon d'automne, Lande aux Moulins), transport (deux lignes de montgolfière + le Sautillon), persistance locale (`WorldSave`, schéma v1, champs `next_id`/`placed` réservés), **grimper universel** (53 arbres, 48 adoptés depuis leurs `MultiMesh` sans un nœud dessiné de plus, tap par rayon caméra, montée sur le flanc de couronne) et récolte (feuilles, coccinelle qui fuit, gland doré à rareté CADENCÉE) ; puis le **lot de cadrage vers `staging`** (5 sept) qui n'a rien créé et a retiré l'emballage : bypass d'authentification supprimé entièrement (`Auth.gd` + `LoginScreen.gd` re-vérifiés **byte-identiques à `origin/main`**), `web-build.yml` restauré à l'identique, `force_ladybug_roll` et l'écriture directe de `WorldSave._data` remplacées par une **graine** de RNG et des `tree_take()` réels, `--showcase` supprimé, les trois outils de développement re-gatés sur `DevTools.enabled()` (liste blanche : hors web, build debug, ou jeton `keepydev` dans l'URL) au lieu d'une liste noire de noms d'hôtes, `CozyGlbInspect` supprimée et les quatre sondes conservées bornées par `ProbeWatchdog.arm()`. Le journal des cinq nuits reste *verbatim* dans [`docs/CARTE_BLANCHE_JOURNAL.md`](../CARTE_BLANCHE_JOURNAL.md) | [CH26_MONDE_COZY.md](CH26_MONDE_COZY.md) | 1 | 182 | 4 → 5 sept 2026 | `staging` |
| CH27 | Karting lot 1 — quatrième zone (« le Circuit »), kart en conduite libre au pouce (ancre horizontale, accélérateur automatique, second doigt = frein), circuit Catmull-Rom 230,7 u à six virages, chrono au tour à meilleur temps persistant (`WorldSave.kart.best_ms`, additif) ; architecture `KartInput`/`KartBody`/`KartTrack`/`KartLap`/`KartTouchInput`/`KartHud`/`KartDecor` + module `HubKarting`, chacune déjà générique pour N karts (contrat écrit pour le lot 2 : adversaires IA, `add_racer`, classement, rubber-band, personnalités par animal) ; régression réelle trouvée et corrigée sur `HubCamera` (variable-ombre de `global_position` en mode conduite, invisible à la sonde du lot, trouvée par la table des rides existants rejouée contre une référence importée à part). Version cadrée de la section « V7 — KARTING LOT 1 » du journal, conservé *verbatim* dans [`docs/CARTE_BLANCHE_JOURNAL.md`](../CARTE_BLANCHE_JOURNAL.md) **Lot 2 (V8, même jour)** : HUD conduite centré, trois adversaires IA (`KartAiDriver`, profils chat/castor/sanglier), course à feux 3 tours, classement, résultats persistants, collisions, piste 10 u, chat/castor/faon à la masse de Keepy — section « LOT 2 » du fichier + journal « V8 — KARTING LOT 2 » | [CH27_KARTING_LOT1.md](CH27_KARTING_LOT1.md) | 2 | 170 | 5 sept 2026 | `main` (les deux lots) |
| CH29 | La Crique — **cinquième zone** du hub, à l'EST de la Lande aux Moulins par un couloir piéton (x 38..44, z −100..−92, porte (41, −96)) ; sable, mer turquoise concave (disque r 48, rive x 60–64), phare rouge et blanc, palmiers, parasols, bouées ; interaction propre : **châteaux de sable** (3 emplacements, 3 étapes, fondent sous pluie/orage) ; météo : lampe et faisceau du phare sous pluie/orage, bouées au vent ; réseau étendu : **ligne Corail** (plateau (−13, −33) → Crique (52, −98), 10,6 s) ; nouveau transport : **char à voile** (glisse libre continue au sol, 10,7 u/s au soleil, ×1,25 orage, second véhicule du modificateur de hop, non re-garé, position persistée) ; `WorldSave` **schéma 2** avec migration 1 → 2 ; `HubWorld._gates_between` devient un ARBRE (`BRANCH_OF`) ; terrier de dune + `ModelSlot` inerte (0,80 × Keepy) pour un futur habitant ; `CoveProbe` 163 checks, cinq passes rouge-avant-vert ; temps de trajet spawn → Crique 30,6 s → 18,3 s, Crique → Circuit 15,3 s → 7,5 s | [CH29_CRIQUE.md](CH29_CRIQUE.md) | 1 | — | 5 → 6 sept 2026 | `main` |
| CH30 | Conduite unifiée — **difficulté mesurée avant d'être touchée** (`RaceBalanceProbe` : temps au tour de chaque adversaire, vitesse de pointe réelle, part du rubber-band, écart à l'arrivée ; trois prémisses tombées — la laisse est INERTE (0,1 s sur 75 s), `a_lat` SATURE sur la limite de braquage à l'oméga (4,17 u/s), l'échelle est COMPRESSIVE, d'où un troisième preset x2.5 et non x2) ; trois presets `KartDifficulty` commutables à chaud derrière `?keepydev=1` sur le patron V7b, calibrés sur un dénominateur mesuré — le tour le plus rapide que le véhicule peut tourner ici, 21,633 s — défaut x1.5 (déficit ×0,630, cible 1/1,5) ; relevé dev des tours de tous les concurrents ; **extraction de `VehicleDrive`** prouvée byte-identique à `origin/staging` sur 5 400 frames par `KartTraceProbe` après correction d'un piège de TYPE (`rotation.y` est un float32, un `float` GDScript un float64) ; **char à voile piloté en continu** par le même `KartTouchInput`/`KartInput`/`VehicleDrive` et la même caméra de poursuite, rider sur le pont, allure de CH29 au mètre (10,40 u/s soleil, 12,99 orage) ; **garde circuit** comme propriété du sol conduisible, survivant à une sauvegarde empoisonnée ; `ChaseAudit` (160 frames, 5 zones × 8 azimuts × 4 météos, xvfb/opengl3, INCONCLUSIVE sous headless) : pilotes adverses cullés à 52 u sous une caméra de 120 u corrigés, **cyprès à luminance 0,0692 — sous le sol à 0,0799** corrigé par gain uniforme ×4,4 sans toucher le `.glb`, et deux étalons de la sonde corrigés avant le monde (signe de l'enroulement, échantillon de ciel) ; P2 sensoriel sacrifié | [CH30_CONDUITE.md](CH30_CONDUITE.md) | 5 | 431 | 6 sept 2026 | `main` |
| CH50 | Extension zone 0 nord — le sol du skatepark : disque r=28 unioné sur le milieu du bord nord (le MÊME centre que le lobe CH16, qu'il avale à tous les centres que le budget autorise, donc « goulot entre les deux disques » n'est pas une forme dessinable), pire paire créée 106,590 u / **20,117 s** qui PERD contre celle de CH38 (111,414 u / 20,967 s) donc pire traversée du hub inchangée, diagonale reproduite à la frame près (1 122 frames / 18,700 s) ; `COVER_MAX.y` 47 → 63 et les TROIS orthographes du littéral 50 du mur unifiées en un `WALL_NEAR_Z` dérivé (68) ; trois passes rouges (9 / 3 / 3) et le couplage tapis-mur prouvé par les deux dernières ; faux-vert du lot : la sonde en `--headless` lisait **2 743 transforms de `MultiMesh` sur 2 743 en identité** et comptait zéro en vert ; table des sondes rejouée sur deux arbres, parité rétablie, plus la trouvaille que le gate de contraste des plaques CH48 est un test du SOL (`origin/main` porte déjà 0,21 % de sol peint sous L 0,10, son plus sombre à 2,31:1 exactement) | [CH50_ZONE0_NORD.md](CH50_ZONE0_NORD.md) | 1 | 402 | 8 sept 2026 | — |

**Total chantiers : 137 sections, 25 159 lignes.**

⚠️ **CH20 contient DEUX sections écrites rétroactivement par le LOT H**
(LOT A — identification de l'ours par rendu ; LOT F — orientation de l'ours
au repos). Ces deux lots existaient en code sur `staging` mais n'avaient
jamais reçu de section. Elles sont reconstituées **depuis le message de
commit et le diff réel**, pas depuis un souvenir, et chacune le déclare en
tête pour qu'un futur lecteur sache d'où elle vient.
| CH31 | La course cesse d'être gagnée d'avance — **le référentiel de mesure était faux, et c'était le lot** : `RaceReconProbe` a pris les trois hypothèses du brief dans l'ordre — (c) réfutée (le banc charge bien `HubWorld.tscn`, `circuit_1`, 230,711 u, même plateau), (b) réfutée (un adversaire seul contre le peloton : **≤ 0,117 s** de meilleur tour, laisse **≤ 0,13 s**, rubber-band inerte pour la seconde fois), (a) **CONFIRMÉE** — l'étalon `human_ref` de CH30 est un profil de `KartAiDriver`, donc le MÊME contrôleur que les adversaires, et il tournait **24,400 s contre les 23,350 s du chat qu'il mesurait** ; plafond trouvé : le profil de vitesse était bâti sur la courbure de la **SPINE** alors que le pilote roule jusqu'à 3,9 u à côté, ce qui épinglait **tout le plateau à 4,17 u/s à l'oméga** — reconstruit sur la courbure et la longueur d'arc de la ligne réellement planifiée (lissage mesuré : 0 passe → 31,150 s, **12 → 22,767 s**, 40 → 23,250 s), `corner_bias` du sanglier 1,1 → 0,70 et `a_lat` 4,6 → 5,2 parce qu'un balancement de ligne n'est plus gratuit ; plancher re-dérivé **PILOTÉ** et non déduit (la ligne de courbure minimale a été jetée : `Σ|Δ²P|²` pondère un virage par `ds⁴`, donc elle a déplacé le rayon minimal **dans le mauvais sens**) — **21,633 s → 18,583 s**, plein gaz 12,111 s ; nouvel étalon `HumanRefDriver` (aperçu 1,15 s, 9 décisions/s, bruit gaussien de visée et de braquage, **latence tirée une fois par run**, **n = 320**, publié en distribution p10/p50/p90) : p50 **21,633 s** sans boost et **20,350 s** avec ; table de difficulté **reconstruite et non décalée** — `top` est un plafond dur donc il porte la plus grosse échelle (mesuré : `pace` 1,20 → 1,38 déplaçait le chat de **−0,05 s**), vitesse et agressivité en colonnes séparées (`pace`/`top` contre `headroom`/`bias`/`a_brake`/`wobble`/`fault`), meilleur tour adverse **21,850 / 20,233 / 19,300 s** contre 24,350 / 23,350 / 22,733 en CH30 (**3,1 à 4,3 s au tour** gagnées au défaut, presets séparés de **1,617 s** puis **0,933 s**), l'étalon est **battu de 0,117 s** au preset par défaut sur une course de 65 s ; départ : le joueur partait en **POLE** (entrée 0 = slot 0, 1,8 u devant le dernier) — `grid_slot()` inverse le plateau et `KartProbe` lit désormais l'accesseur au lieu de supposer l'identité ; **accélérateur : il EXISTAIT** (boost V7b) mais l'affordance n'apparaissait qu'un doigt déjà posé, lever le doigt la tuait, et elle coûtait 150 px — jauge permanente remplie à la VITESSE, flèche qui s'efface, relâchement dégressif sur 0,45 s, course ramenée à 105/14 px, valeurs rendues **par instance** pour que le char à voile garde les siennes (`YachtTraceProbe` : 24 lignes de trace **byte-identiques** entre les deux arbres, blind check vert) ; vitesse générale 13,0 → **15,0 u/s** (boost 16,5 → 19,05, même rapport) avec la **borne mesurée à 16 u/s** — un croisement, au-delà duquel le pilote qui pousse tourne PLUS LENTEMENT que le discipliné (20,917 contre 20,467) en passant un quart du tour hors du ruban — et le plafond de boost seul prouvé **non-levier** (+0,10 s pour deux fois plus de hors-piste) ; `TRACK_ID` `circuit_1` → **`circuit_1b`** (record remis à neuf sans invalider la sauvegarde) ; relevé dev enrichi (moyenne, pointe réelle, temps en contact) ; 25 checks, 0 échec à n = 320, dont **blind B (la latence est câblée : 1,383 s)**, D4 (aucun adversaire à plus de 0,80 tour au drapeau — rouge à 1,156 tour à la passe rouge) et D6, né d'un échec de blind C : x2.5 à `pace` 1,55 tournait **sous le plancher en roulant 131 frames hors du ruban** ; ⚠️ **D5 est passé VERT avec le correctif neutralisé** et a dû être réécrit — le rang à six secondes ne savait pas distinguer les deux grilles, il est désormais publié non gaté | [CH31_DIFFICULTE.md](CH31_DIFFICULTE.md) | 1 | 389 | 6 sept 2026 | `main` |
| CH32 | Fiabilité des sondes après la promotion palier 2 — lot de diagnostic pur, sans changement de gameplay. **SUJET 1** : `WaterTintProbe` assertait encore `discs().size() == 4` alors que `HubWater` construit **cinq** disques depuis CH29 (`sea` ajouté) — diagnostic confirmé en lisant `HubWater.gd`, corrigé à 5, rouge-avant-vert prouvé (disque `sea` neutralisé → 10 échecs au lieu de 9, fichier restauré `cmp` byte-identique), aucune autre sonde ne porte ce contrat (`HubWater` n'a qu'un seul point d'instanciation). **SUJET 2** : cinq sondes rejouées isolément — `CoveProbe` **PASS 179/0**, ne reproduit pas le gel rapporté (contention de ressources probable au rejeu complet) ; `LakeZoneProbe` et `V6CrittersProbe` rendent une **INCONCLUSIVE propre** à leur propre budget (900 s / 600 s) après une progression réelle et continue (CPU 175-196 % actif, dizaines de checks verts) — cause (i) limite d'environnement réelle : rendu logiciel llvmpipe (aucun GPU dans ce sandbox), le `ProbeWatchdog` fonctionne exactement comme conçu ; `SeesawProbe` **FAIL déterministe et rapide** (157 ≠ 144 draw nodes), pré-existant depuis CH26, pas une inconclusion ; `SubstituteModel.tscn` n'est **pas un probe** (fixture nue sans script, déjà exclue par `ProbeTimeoutAudit.gd:61`) — lancée comme scène elle boucle indéfiniment (aucun `quit()`), défaut de sélection de scènes de l'outillage de rejeu, pas du code. Aucune régression de jeu trouvée, aucune escalade Opus recommandée | [CH32_SONDES_FIABILITE.md](CH32_SONDES_FIABILITE.md) | 2 | 90 | 6 sept 2026 | `staging` |
| CH33 | Voilier — troisième véhicule coordonné par `HubTransport` (`VEHICLE_SAILBOAT`), pilotable sur la mer que CH29 a déjà construite (`SEA_RADIUS`/`SEA_CENTRE` non touchés, agrandissement en lot séparé) ; `SailBoat.gd` calqué sur `SandYacht.gd`/CH30 (même `VehicleDrive.step()` non modifié, même `KartTouchInput`, même caméra de poursuite), coque/voile réutilisant `yacht_hull_0.glb`/`yacht_sail_0.glb` (aucun asset créé) ; **échouage progressif et réversible** remplace le mur dur du char à voile — une traînée continue (`ground_factor_at()`, lue sur `HubRegion.shore_distance` déjà publié) appliquée à la vélocité APRÈS `VehicleDrive.step()`, jamais un clamp de position, ce qui rend la marche arrière toujours capable de ramener le bateau à l'eau (mesuré : 295 frames depuis échoué) ; rouge-avant-vert en DEUX passes (mécanisme neutralisé → 1 échec sur la friction seule ; `GROUND_DRAG_LAMBDA` porté à 500 → les 3 assertions d'échouage tombent, dont la réversibilité, prouvant que cette assertion sait échouer) ; **mouillage fixe, aucune persistance** (`SAILBOAT_MOORING`, pas de champ `WorldSave`) ; `SailBoatProbe` neuve, 42 checks 0 échec, après correction de deux bugs DANS LA SONDE (le frein doit être réaffirmé chaque frame physique — `KartTouchInput` le remet au clavier sinon — et un point de test placé hors de la mer par erreur) ; `WaterTintProbe` et `CoveProbe` rejouées sous xvfb/opengl3, 0 régression (les 9 échecs de `WaterTintProbe` et l'INCONCLUSIVE de `CoveProbe` reproduits À L'IDENTIQUE sur `origin/staging` non modifié, dans un `git worktree` séparé) ; toolchain Godot 4.3 absent du sandbox, provisionné en session (éditeur et templates vérifiés byte-exacts contre les tailles publiées par CLAUDE.md) | [CH33_VOILIER.md](CH33_VOILIER.md) | 1 | — | 6 sept 2026 | `staging` |
| CH35 | Socle multi-altitude — **dépôt documentaire fidèle** (lot doc CH36, commit additionnel) de trois dossiers produits le 7 sept 2026 par trois sessions de lecture/calcul et livrés en chat, qu'aucune ref du dépôt ne portait (CH35-B et CH36 le signalaient) : **CH35-CONCEPTION** (dossier d'architecture : inventaire du couplage à y = 0 (~30 écritures de y), le « verrou » `VehicleDrive` qui n'en est pas un, noyau `scripts/nav` réutilisable sur ses idées et pas sur son abstraction, frontière hub/montagne option A vs B, amendement caméra « point sol = (x, h, z) », ~25 sondes qui resteraient vertes sans rien prouver, budget triangles, sauvegarde, découpage en lots 0 → 4) ; **CH35-B** (complément : plafond de cadre mesuré 7,968 u sur la vraie caméra — `HubTrees.gd:96` faux de 1,01 u, inventaire complémentaire des écritures de y dont quatre suiveurs globaux, shader de sol sur mesh non plan par calcul, protocole de mesure device en 10 stations avec grille de décision, eau en altitude, accesseur `HubSurface` multi-domaines) ; **CH35-C** (rendu de terrain test : 50 captures offscreen SUN/RAIN, 4 buttes + 1 mesa — l'étirement 1/cos est invisible jusqu'à 45°, le vrai défaut est l'absence de shading, pente max recommandée 30° marchable / 45° flancs courts / > 55° interdit ; plan d'exécution du lot 1 SURFACE en 6 vagues, site par site, avec `SurfaceProbe` phases A → G) ; déposés **tels quels**, dans l'ordre, sans réécriture ni mise à jour de chiffre — certains ont depuis été tranchés par CH36 | [CH35_MULTI_ALTITUDE.md](CH35_MULTI_ALTITUDE.md) | 3 | 743 | 7 sept 2026 | `staging` |
| CH37 | Socle multi-altitude — **LOT 1 SURFACE** : `HubSurface.gd` publié (requête pure au patron `HubWater` : ne refuse aucun tap, ne clampe rien, ne bouge rien), `ground(flat)` **orthographe unique du point sol**, échantillon barycentrique sur la grille `PackedFloat32Array` (float64 REFUSÉ, pas converti) et sur la MÊME diagonale de triangulation que le mesh du lot 2, AABB disjointes (donc aucun blend à écrire), raccord **C0 exact** au périmètre marché à 0,5 u (`|h| < 1e-4` — une MARCHE est une transition de niveau, pas une pente), refus d'une AABB coupant une bande `CozyPalette` lue et non recopiée, `intersect_ray` **byte-identique** à `Plane(UP, 0)` table vide (20 rayons, écart 0.000000000), `normal_at` absent ; six vagues du plan CH35-C exécutées dans l'ordre (marche `KeepyHopper` — aucun littéral `0.0` de ligne de base ne survit —, caméra `HubCamera` ×3 sites, tap `HubTapInput`, retours au sol `KeepyHopper` + `HubWorld`, pluie et ombre `CozyScatter` dont le `lift` qui devient `p.y − h`), **ZÉRO domaine enregistré en jeu** donc un no-op arithmétique ; `SurfaceProbe` permanente phases A → G, **blind check `height_at ≠ 0` en tête de CHAQUE phase** ; ⚠️ la phase TAP est passée **VERTE contre le correctif neutralisé** et a dû être réécrite (deux causes : `tapped_ground` porte LA POSITION D'UN ARBRE quand le tap en frôle un, et comparer « à plat » vs « avec relief » déplace AUSSI la caméra — 0,143 u contre 5,673 u pour la comparaison honnête) ; trois écarts au plan nommés (delta XZ explicite sans quoi `ARRIVE_EPSILON` ne termine jamais une marche en pente, cinq `leave_*` et `tree_foot_point` que la table ne listait pas, `_badger_rest` branché à l'accesseur et non à ses deux appelants) | [CH37_SURFACE.md](CH37_SURFACE.md) | 1 | — | 7 sept 2026 | `staging` |
| CH38 | Zone montagne — **LOT 2 PARTIE A** : le premier domaine reel enregistre dans `HubSurface` (CH37 n'en enregistrait aucun), la **crete ouest** — rectangle x [-63, -35] / z [-12, 18] choisi par MESURE des huit degagements (le plus serre : la berge du petit lac a 0,85 u), boulonne sur le bord ouest du plateau avec un perimetre a **exactement 0** donc aucune marche, aucune porte, aucun numero de zone ; deux cosinus sureleves a **support compact** (raccord C1, pas seulement C0), sommet **4,500 u**, pire pente **27,834°** mesuree sur les triangles DESSINES ; ⚠️ la largeur est fixee par la TRAVERSEE et non par le relief — un rectangle sur un BORD allonge la pire paire (contrairement a un lobe centre SUR un bord), **20,967 s marches** contre un plafond de 22 s, et la version a x = -71 sort a **22,383 s** en passe rouge ; ⚠️ deux bosses qui se recouvrent **additionnent leurs gradients** (33,0° mesures avec deux bosses a 23° et 17°) ; budget triangles gate **des le premier commit** : **+1 744** primitives pire cas sur 8 stations × 2 cameras contre un plafond de 10 000, et un delta **NET** de −21 a +1 023 mesure sur DEUX ARBRES (le mur de foret s'ecarte de la region) ; `MountainProbe` 7 phases ALL GREEN avec blind check et **cinq passes rouge-avant-vert** restaurees byte-identiques ; rendus offscreen SUN/RAIN aux quatre stations, dont la plus faible (pied nord, versant a ~90 % du cadre) signalee sans etre maquillee ; **zero semis, prop, eau, critter, transport** | [CH38_MONTAGNE_RELIEF.md](CH38_MONTAGNE_RELIEF.md) | 1 | — | 7 sept 2026 | `staging` |
| CH39 | Le relief disparait quand Keepy marche dessus — **DIAGNOSTIC d'abord, correctif ensuite** : les deux stations device de Mathieu reproduites a l'identique en sandbox, les cinq hypotheses du brief tranchees UNE PAR UNE avec preuve (mesh/`height_at` coherents a **8 x 10^-6 u** par une chute de rayon Moeller-Trumbore independante de `_sample` ; le plan y = 0 ne cache RIEN — le masquer change le compte de **13 898 -> 13 898** et **14 -> 14** ; le mesh est exactement dans le rectangle declare ; Keepy etait bien a **3,9484 u** de haut, MARCHE et pas teleporte, avec la camera a 7,600 u au-dessus du sol local aux DEUX stations) ; ⚠️ **CAUSE prouvee a variable unique — le treillis etait ENROULE A L'ENVERS** : Godot tient les faces HORAIRES-A-L'ECRAN pour faces AVANT, un produit vectoriel main droite +Y se lit anti-horaire depuis toute camera au-dessus, donc `cull_back` jetait toute la colline — `cull_back` **14 px** contre `cull_disabled` **317 646 px** (x 22 689) ; le « dome net decoupe contre les arbres » etait **l'arriere de la colline vu par transparence** ; ⚠️ **DIXIEME faux-vert, cinq mecanismes empiles** : PHASE C assertait la convention MATHEMATIQUE en citant la convention MOTEUR dans son propre commentaire (verte 1 680/1 680), PHASE D et F lisent la GRILLE, et **PHASE E compte les primitives SOUMISES — le back-face culling est en aval du compteur**, donc « the ridge DOES cost something » etait vrai et signifiait l'inverse ; **PHASE G** ajoutee : passe d'identification masquee, blind check d'abord, et un gate SANS SEUIL — « le test de face ne jette rien » (rouge-avant-vert : **5 rouges, exactement PHASE C + PHASE G**, restauration byte-identique, puis ALL GREEN 0 red) ; trouve sans etre corrige : la cretes est **CHAUVE** (`COVER_MIN/MAX` s'arrete a x = -37, le domaine commence a -35) et le sol unlit ne porte **aucun signal de pente** (Pearson r = +0,098 / -0,342) | [CH39_RELIEF_DIAGNOSTIC.md](CH39_RELIEF_DIAGNOSTIC.md) | 1 | 181 | 7 sept 2026 | `staging` |
| CH40 | Zone montagne — **LOT 2 PARTIE B** : l'habillage de la crete. **ETAPE 0 = INVENTAIRE, et rien n'a ete genere** — le depot possedait deja toute la montagne (`tree_4_conifer` 390 tri en vert-BLEU 0,06/0,24/**0,10** la ou l'arbre rond du plateau est en vert-JAUNE 0,07/0,25/**0,04** : meme luminance, autre teinte ; `cypress_0/1` 140 tri, L = 0,005, dont `CozyPalette.FAMILY_GAIN` porte deja le gain 4,4 **cle sur le nom de MESH**, donc herite sans une ligne ; `palerock_0/1`, **la seule pierre du depot qui ne soit pas vert-mousse** ; `pebble_0/1`) ; **ETAPE 1** : `COVER_MIN.x` valait −37 pour un domaine a −63 — **20 instances** et **12 des 16 cases vides** mesurees sur l'arbre livre, borne desormais **lue sur `HubRegion.MOUNTAIN_MIN.x`** et non retapee, `_sprinkle` posant sur `HubSurface.ground(p)` **pris EN DERNIER** (les gardes au-dessus comparent des distances 3D a des centres a y = 0 : lever `p` avant elles les relacherait toutes) et `_shadow_at` **n'emettant aucun blob sur un domaine** (un quad horizontal plat flotte de 0,27 u sur une pente a 28°) ; **ETAPE 2** : composition et non semis — couronne de neuf sur un anneau de 5 u, epaule, quatre fleches en bande mediane, **douze blocs pales biaises au NORD** (le versant que CH38 avait nomme le plus faible), eboulis sur les hauts, ⚠️ **la couronne a eu besoin de sa PROPRE separation** (neuf pieces a 3,42 u pour 4,2 u reserves : l'anneau s'est refuse lui-meme, 4 coniferes sur 5) ; **densite publiee** : habillage **0,0571/u²** = **0,75×** le semis plateau hors herbe et **0,63×** les props du vallon, total domaine **0,1643/u²** = **0,37×** le plateau via `DOMAIN_COVER_KEEP = 0,25`, la seule ligne plus dense etant l'arbre (1,19×) parce qu'il est le seul a faire une silhouette ; **budget dans le fichier des le commit** `RIDGE_TRIANGLE_BUDGET = 6000` (44 043 device + 6 000 = 50 043) : **5 875 soumis**, **+1 854 mesure** pire cas, delta **NET sur DEUX ARBRES +2 971 / +4 436** contre +5 721 / +7 984 sans l'amincissement ; ⚠️ **ONZIEME FAUX-VERT, et il etait dans la sonde** — avec la passe arretee `_ridge_nodes()` est VIDE, donc « cacher et relire » ne cachait rien et le compteur bougeait quand meme de **+64** : « the dressing costs something » verte sur une colline nue, ferme par un **plancher de bruit mesure** (jusqu'a 140) et la passe rouge rejouee rend **7 rouges au lieu de 5** ; `MountainProbe` 11 phases, **78 assertions ALL GREEN 0 red**, **sept passes rouge-avant-vert** restaurees byte-identiques | [CH40_MONTAGNE_HABILLAGE.md](CH40_MONTAGNE_HABILLAGE.md) | 1 | 297 | 7 sept 2026 | `staging` |
| CH41 | Zone montagne — **LOT 3 : LA LUGE ELECTRIQUE, premier vehicule sur SURFACE**. Pas un ride : un **quatrieme vehicule** tape/monte/pilote au pouce comme le kart, libre partout sur la carte, qu'une pente pousse. **LE VERROU : `VehicleDrive.gd` N'A PAS ETE OUVERT** — voie 1 du brief, `max_speed` module par instance, les quatre fichiers de conduite **byte-identiques** a `origin/staging` (`KartBody.gd:218` compris). Deux mesures le referment : `off_lambda` est un **amortissement** (plafond laisse plat, la descente atteint **10,358 u/s** contre `MAX_SPEED_FLAT` 8,50) et le plafond leve avec la pente donne **15,253 u/s, +47,3 %, x1,79 le cap moteur**. ⚠️ **MAIS CH35 Q2 SE TROMPAIT SUR L'ORDRE** : force injectee AVANT `step()` = vehicule **GELE A EXACTEMENT 0,000 u/s** face a la montee (la force rend `v_fwd` negatif, la branche « reversing and the throttle comes back » le ramene a zero et jamais au-dela, et a vitesse nulle il n'y a **aucune autorite de braquage** — le blocage de `SandYacht._wall` etape 3 par l'ordre des operations) ; corrige en injectant **APRES**, comme `SailBoat` le fait deja pour son echouage, et l'inegalite `climb_authority()` > `slope_force()` est **publiee et gatee** (10,53 contre 13,60, 77 %). **`HubSurface.normal_at` arrive**, exactement comme CH37 l'annonçait : `gradient_at` primitive, derivee **analytique** de `_sample` donc **la normale de face du mesh dessine**, verifiee sur les **1 680 triangles committes** (pire ecart **1,2e-4**, blind a 9,2e-3), **facettee** (marches de **11,168°**) et lissee par le consommateur (chassis **3,881° derriere a 13,533 u/s sur un sol a 19,810°**). **Mesh PROCEDURAL, aucun asset genere** (inventaire d'abord : rien de forme luge dans 107 `.glb`), **60 triangles**, enroulement **decide** par `_quad()` et non ecrit a la main. **Budget** : la luge n'est PAS dans `RIDGE_TRIANGLE_BUDGET` (cette ligne parcourt les batches de `CozyScatter` ; un `Node3D` s'annule des deux frames du delta) — cout propre **exactement +60 primitives** aux **11 stations sur 16** ou le compteur est immobile, et 60 <= **125**, la marge CH40. **Camera de poursuite en pente exercee pour la premiere fois** : pire degagement **1,0243 u** sur 7 192 paires, blind a −2,976. ⚠️ **TROIS faux-signaux trouves DANS la sonde — douzieme, treizieme et quatorzieme du depot** : (1) test d'enroulement contre le centre de masse d'un assemblage **non convexe** → 46/60 declares faux sur un mesh que le rendu prouvait juste (`cull_back` = `cull_disabled` = **13 190 pixels**) ; (2) `slope_gain` mis a **ZERO** laissait « DESCENT IS FASTER » **VERTE** a 12,891 contre 8,081 — le plafond releve portait tout, ferme par une **paire en roue libre** gatee sur le **SIGNE** de l'avance (**+8,89 u** avant en descente, **−5,35 u** arriere en montee) ; (3) le chassis neutralise lisait **0,0000° d'ecart** parce que la course atteignait le **plat**, ferme par un blind sur l'inclinaison du sol au moment de la lecture. Plus une jambe trop longue qui rendait la **montee plus rapide que la descente** (19,597 u/s : elle franchissait le sommet), fermee en gatant le signe de la pente **aux deux bouts**. `SledProbe` 11 phases, **ALL GREEN 0 red**, **six neutralisations** (huit runs) rouge-avant-vert, restaurees byte-identiques | [CH41_LUGE.md](CH41_LUGE.md) | 1 | 220 | 7 sept 2026 | `staging` |
| CH42 | **Marche arriere transverse + diagnostic du blocage falaise**. **ETAPE 1, LE DIAGNOSTIC, RENDU AVANT TOUT CODE** : les quatre hypotheses du brief mesurees sur les **trois vrais murs** de la crete (28 points x 2 braquages x 600 frames). Mur **ECARTE** (28/28 points interieurs `drivable`, 28/28 points a 1 u au-dela refuses) ; pente **ECARTEE** (un bord de crete est **plat par contrat** -- `HubSurface` refuse un domaine dont le perimetre n'est pas a 0 : pire echantillon 0,0865, **2,19 u/s2 contre 13,60** d'autorite de montee) ; coin mort **CONFIRME mais pour les coins seulement** (4 runs sur 56 ne s'eloignent **jamais** de 4 u en 10 s) ; et **LA CAUSE N'ETAIT DANS AUCUNE DES QUATRE** : `VehicleDrive` gagne le lacet sur `|v_fwd| / steer_full_speed`, or **c'est `v_fwd` que le mur mange** -- pire epingle **(-62 ; 17,7)** : 600 frames de braquage a fond, vitesse avant moyenne **0,0851 u/s**, gain **0,0284 (2,8 %)**, excursion maximale **1,04 u**, contre **59 frames** pour 4 u en terrain libre. **AUCUN CORRECTIF DE MUR N'EST NECESSAIRE** : `HubRegion.gd`, `HubSurface.gd`, `SandYacht._wall` non touches, et PHASE ESCAPE le prouve dans l'autre sens. ⚠️ **QUINZIEME FAUX-SIGNAL, ET IL ETAIT LA METRIQUE** : « bloque » mesure par la distance au depart apres 600 frames a rendu **quatre fausses epingles** -- un braquage tenu dessine un **cercle de 36,7 u a 352,7 deg** qui **revient d'ou il part** ; et la meme erreur moyennait la CAUSE sur tout le run (donc sur la boucle libre) et publiait un gain de **0,59-0,67**, deux versions durant. Ferme en notant chaque run sur la **PREMIERE FRAME** ou il atteint un rayon d'echappement, et en moyennant l'etat **sur la fenetre ou l'etat tient**. **ETAPE 2** : `KartInput.reverse` (float 0..1 tenu, **a cote** de `throttle`), **une branche `elif`** dans `VehicleDrive.gd` -- fichier en **addition PURE, 40 lignes, zero ligne retiree** -- dont la moitie « encore lance vers l'avant » est **l'arithmetique du frein enonce pour enonce** ; le braquage n'est **pas** touche (la ligne `v_fwd < -0.05` lisait deja la velocite et non l'input). `REVERSE_ACCEL` par vehicule : kart **6,00** = `BRAKE_DECEL x 0,4`, donc le kart recule **exactement comme avant** ; char **6,00** ; voilier **4,00** ; luge **13,00**, et ce n'est pas un nombre de feeling -- `SledBody.reverse_authority()` est gate contre `slope_force` (**10,5312 contre 13,0000, 81 %**) parce que reculer nez a l'aval **c'est monter**. **Gain mesure** sur 112 epingles : temps moyen pour s'eloigner de 4 u **439,1 -> 119,7 frames**, pire cas **jamais -> 143 frames**, le coin mort **1,04 u en 10 s -> 4 u en 118 frames**. **Byte-identite prouvee sur DEUX ARBRES** : `KartTraceProbe` 186 lignes identiques (3 tours), trace de mouvement char/voilier/luge 900 frames **identique** (blind : `BRAKE_DECEL` +0,001 fait bouger **36 lignes**), `YachtTraceProbe` differe sur **2 lignes / 54** -- la colonne `brake` au frame 240, c'est-a-dire exactement la separation d'input du lot. `SledProbe` ALL GREEN 0 red (xvfb+opengl3), `SailBoatProbe` 42/0, `KartProbe` 150/0, `ProbeTimeoutAudit` 82 scenes PASSED. `ReverseProbe` 7 phases **ALL GREEN**, passe rouge **19 rouges attendus, 19 obtenus, aucun autre**, restauree byte-identique. ⚠️ **DETTE** : le second doigt ecrit desormais `reverse` et **rien ne l'annonce** -- le defaut CH31 de l'accelerateur qui se rejoue | [CH42_MARCHE_ARRIERE.md](CH42_MARCHE_ARRIERE.md) | 1 | 166 | 7 sept 2026 | `staging` |
| CH43 | **Marche arriere : un seul geste, meme axe qu'avancer.** Corrige un choix UX de CH42 apres essai device de Mathieu : le **deuxieme doigt disparait**, l'axe vertical de l'accelerateur devient **SIGNE** -- haut = allure (V7b/CH31 inchange), bas = `input.reverse` sur le **miroir** du meme span. **DEUX CHAMPS, PAS UN AXE SIGNE**, et c'est un choix de rayon d'explosion et non d'elegance : `throttle` est ecrit par `KartTouchInput`, `KartAiDriver`, `KartLineInput` et **chaque `set_all` de sonde** -- un signe dessus aurait mis un retour UX dans un composant partage. **Les quatre vehicules l'ont sans une ligne de leur cote** : `KartTouchInput` est le seul ecrivain tactile du depot et il n'en existe que **deux instances** (`HubKarting`, `HubTransport`). Symetrie mesuree aux **quatre quarts** du span et pas seulement aux bouts, ecart pire cas **0,000000000** ; relachement **asymetrique et delibere** (le boost se dissipe sur `boost_release_s`, le rapport coupe net -- aucune raison de CH31 ne survit sous l'ancre). **`VehicleDrive.gd` N'A RECU AUCUN CHANGEMENT DE COMPORTEMENT** : le garde-fou etait deja le `if v_fwd > 0.3` de CH42, il a seulement recu un **nom**, `REVERSE_ENGAGE_SPEED`, valeur au chiffre pres. **SEUIL MESURE SUR LE VEHICULE, jamais relu dans la constante** : 30 lancements par vehicule, chaque frame classee par l'**arithmetique** que la branche a produite ; encadrement des quatre **(0,298670 ; 0,303540], large de 0,004870** ; la constante n'est ouverte qu'a la fin pour demander s'il la contient -- passe rouge avec la branche gatee a **0,9** : l'encadrement mesure **s'est deplace a 0,9**. **Garde-fou prouve, fenetre TROUVEE et non choisie** (kart bande atteinte a la frame 48, char 46, voilier 54, luge 40) : zero frame negative avant, zero frame hors branche frein, zero remontee de vitesse, et le run finit quand meme en marche arriere. ⚠️ **LE TEST DE SIGNE SEUL N'AURAIT RIEN VU** : la passe rouge qui **supprime le garde-fou** laisse `negatives_before` **VERT** -- le vehicule ne passe toujours pas negatif avant la bande, il decelere juste a la rampe au lieu du frein. Seule la classification par branche l'attrape. **Physique CH42 au chiffre pres depuis l'arret** : rampes 6,0000 / 6,0000 / 4,0000 / 13,0000, terminales -3,5000 / -2,4000 / -2,6000 / -2,2000. ⚠️ **La luge est conduite A PLAT dans les trois phases physiques** -- `SurfaceDrive` injecte la pente **APRES** `VehicleDrive`, donc a `SLED_PARK` (0,7777 u/s2) la rampe se lit **14,4159** et la terminale **-2,3689** : la colline, pas un defaut, mais la mauvaise question ; station plate **gatee contre** la station en pente et les deux doivent **DIFFERER**, sans quoi PHASE SLOPE deviendrait une tautologie. **Deuxieme doigt prouve absent** par evenements reels et non par grep (`_reverse_index` disparu, second doigt ecrit 0,0000, l'ancre continue de piloter pendant qu'il est sur la vitre, bouton droit mort, bouton gauche vers le bas ecrit 1,0000). ⚠️ **SEIZIEME FAUX-SIGNAL, ET IL ETAIT DANS LA SONDE** : la derniere assertion souris relisait un `reverse` laisse a 1,0 par la verification du bouton droit -- elle **relisait l'assertion precedente** et passait VERTE contre l'ecrivain de CH42 restaure verbatim ; fermee en gatant le champ a zero avant le glissement qui doit l'ecrire, **trouvee par la passe rouge**. **Traces CH42 rejouees : AUCUNE NE DIVERGE** -- `KartTraceProbe` 189 lignes identiques, `YachtTraceProbe` 55 identiques (la ou CH42 avait 2 lignes a expliquer, CH43 n'a rien a expliquer), `SailBoatProbe` identique, `SledProbe` identique des deux cotes (xvfb ALL GREEN 0 red ; les 3 rouges headless sont le faux-rouge du driver dummy, mesures sur la base AVANT toute modification), et les **7 phases CH42 de `ReverseProbe` identiques ligne pour ligne**. `ReverseProbe` **11 phases ALL GREEN 0 red**, trois passes rouges **9 / 14 / 8 rouges attendus, obtenus, aucun autre**, fichiers restaures byte-identiques (`cmp`). **Dette CH31 rouverte par CH42 refermee** : le rail de poussee passe desormais **A TRAVERS** l'ancre, fleche a chaque bout, marqueur chaud/froid, ligne d'aide nommant les deux directions | [CH43_GESTE_UNIQUE.md](CH43_GESTE_UNIQUE.md) | 1 | 255 | 7 sept 2026 | `staging` |
| CH44 | **RECON PURE — preparation minimap. Aucune ligne de gameplay ecrite** ; trois sondes jetables creees, executees, supprimees, arbre revenu byte-identique a `origin/staging`. **AXE 1** : la zone EST une donnee — `HubRegion.zone_of` (`:612`) est quatre paires de rectangles nommes, lue en production par **deux sites seulement** (`HubPerfOverlay:239`, `HubWorld:3339-3340`) ; mais **les bords PEINTS sont une seconde orthographe** — `CozyPalette.AUTUMN_EDGE_Z −39` contre `AUTUMN_MAX.y −42`, `MOOR_EDGE_Z −82` contre `−86`, `CIRCUIT_EDGE_Z −132` contre `−134`, `COVE_RECT` couvrant 156 u de plus en x (fondus deliberes, ~4,1 px sur une minimap de 256) ; seule la mer a un proprietaire unique. Le domaine montagne CH38 est **dans la zone 0**, terme d'union et non zone. **AXE 2** : **aucune constante d'etendue globale**, 13 termes d'union a agreger ; etendues **MESUREES au balayage 0,5 u** — a pied x[−63 ; 74] z[−200 ; 47], char+luge x[−63 ; 74] z[**−130** ; 47] (et non −126 : la Crique descend plus bas que la Lande, le calcul a la main etait faux), mer x[60,5 ; 155,5], eau libre du voilier x[64 ; 152], `KartTrack.fence()` x[−48,5 ; 48,5] z[−198,5 ; −135,5], `WORLD_FENCE` ±220/±320 backstop. ⚠️ **le voilier sort du monde marchable de 82 u en x** (aucun mur dur, seulement une trainee) : un cadre sur `HubRegion` le perd hors champ. Cadre a CREER : 219 x 247 tout-vehicule contre 137 x 247 marchable — deux ratios differents, arbitrage ouvert. **AXE 3** : **ZERO groupe Godot** dans le hub (les deux `add_to_group` du depot sont dans Chased et `scripts/nav`) ; dump de l'arbre construit — 1 Keepy, 1 char, 1 voilier, 1 luge, 4 karts, 7 `HubCritter` (4 habitants + **3 pilotes**), 2 `HubActorWalker`, 3 portails, 5 perchoirs + **54 `Carrier`**, 1 seul feu. ⚠️ **citer des chemins de noeuds est pire qu'une dette : la moitie n'existe pas** — ours et blaireau ne recoivent jamais de `.name` (`@Node3D@228` / `@Node3D@230`), deux portails sur trois non plus (`@Area3D@10/@11`), noms derives du compteur d'instanciation ; les accesseurs publics de `HubBuilder`/`HubTransport`/`HubKarting`/`HubCritters` sont la vraie interface, **mais ours et blaireau n'en ont aucun**. Exhaustivite **non garantie** et dit comme tel (dump au spawn, sans interaction). **AXE 4** : **aucun `CanvasLayer`**, tout est frere sous la racine `Control` ; **un seul point d'insertion** couvre pied et vehicule (`KartHud` est un frere `visible = false`) ; le **tiers bas de l'ecran est entierement libre** dans les deux modes, le haut-droit est partage (`WorldHud` a pied, cache en conduite par les quatre vehicules ; `_standings_box` en course). ⚠️ **`SafeArea` n'expose AUCUN inset** — il peint le fond CSS et bascule `content_scale_aspect` ; `HubWorld:672-673` demande **EXPAND**, donc la bande d'encoche (141 px mesures sur device) est **DANS** le canvas et rien ne dit ou. Le widget **doit** porter `MOUSE_FILTER_IGNORE` : les deux `_unhandled_input` du hub tournent apres le picking GUI. **AXE 5** : le **55 722 du brief est une lecture DEVICE et n'est PAS reproduit ici** (llvmpipe n'est pas WebGL2) ; reference mesuree sur le banc CH38/CH40 (xvfb+opengl3, spawn, sun, 1080x1920) **`engine_prims` 70 493 / 295 draw calls**, total tous viewports **71 567 / 311**, donc **toute la couche 2D existante = 1 074 primitives et 16 draw calls**. ⚠️ **aucune des trois approches ne bougera la ligne que Mathieu LIT** : `snapshot()` lit le RID du SubViewport 3D, et `engine_total_*` est **collecte mais jamais imprime** par `_format()`. Les trois options **chiffrees**, plancher de bruit **0/0** : `TextureRect` **+2 prim / +1 call** ; `Control._draw` (40 cercles + 24 lignes + 2 rects) **+2 619 / +43** — **`draw_circle` ne se batche pas, un draw call PAR MARQUEUR** — et le redessiner a chaque frame coute **+0/+0** ; `SubViewport` 256x256 ortho 250 u partageant `world_3d` **+74 085 / +421**, soit **plus cher que la vue principale** (une ortho couvrant la carte a tout dans son frustum, la resolution n'y change rien), et **exactement 0** en `UPDATE_DISABLED`. Ce banc ne dit rien du device, rien du temps CPU d'un `_draw`, rien de la memoire. **AXE 6** : la position du char vit dans `user://keepy_world.json` (IndexedDB sur web), schema 2, cle `cove.yacht`, ecrite en `HubTransport:599` et lue en `:333` ; **`YACHT_PARK (48, 0, −112)` existe deja** et est a **12,033 u** de sable sec du rivage — le respawn demande ne cree aucune constante. **Aucun autre vehicule ne partage le mecanisme** (voilier et luge sont sans persistance par brief explicite ; balle, karts et montgolfieres n'ont pas de position sauvee) : le retirer ne peut rien casser ailleurs. Cout **~40 lignes retirees / ~10 ajoutees**, 8 assertions de `CoveProbe` a reecrire ; `yacht_rides` est un **STAT et reste** ; le garde `drivable` disparait avec la persistance et son assertion doit etre **retiree, pas adaptee** ; bump de schema **non necessaire mais a arbitrer**. Signale non corrige : **CH36 n'a pas de dossier** sous `docs/lots/` | [CH44_RECON.md](CH44_RECON.md) | 1 | 515 | 7 sept 2026 | `staging` |
| CH46 | **LA MINIMAP PERMANENTE.** Un `Control` en coin bas-gauche, **nord fixe**, visible a pied ET dans les quatre vehicules, cadre sur le monde **marchable seul** (`HubRegion.walkable_bounds()`, accesseur neuf : les **13 memes termes d'union** que `contains()`, gate par balayage 0,5 u et exige **SERRE des quatre cotes** -- 137 x 247 u). **ZERO chemin de noeud** : quatre groupes (`MinimapMarkers`) que la carte ENUMERE, plus `minimap_route` pour ce qui se dessine en LIGNE ; **37 marqueurs**, dont les **cinq sans nom** que CH44 avait signales (ours, blaireau, deux portails, bateau du ruisseau) -- l'ours et le blaireau ont recu **une ligne** et rien d'autre. **COUT : l'atlas gagne, et de loin** -- meme banc et meme run que CH44, plancher de bruit **0/0** : atlas (1 quad de fond + 37 marqueurs, **une seule texture**) **+76 prim / +1 CALL**, contre `draw_circle` **+2 370 / +38** -- soit **un draw call pour toute la carte**, la ou l'alternative en fait **un par marqueur**. Frontieres **PEINTES** gatees au pixel (automne -38,13 contre -39 peint / -42 logique ; lande -81,35 ; circuit -131,63 ; 0,882 u/px). **Clamping GTA** prouve sur le voilier pousse a 82 u hors cadre : rabattu au bord, **forme carree** au lieu du disque, remplissage des coins **0,775 contre 0,284**. `MOUSE_FILTER_IGNORE` prouve par **evenements reels** avec la paire IGNORE/STOP (2 evenements et `tapped_ground` / 0 et 0 / 2 et 1). ⚠️ **DIX-SEPTIEME FAUX-VERT, DANS LA SONDE DE CE LOT** : la phase des frontieres lisait le **TRAIT** et non le **REMPLISSAGE**, et la passe rouge qui met les bandes sur les bords LOGIQUES est sortie **ALL GREEN** ; referme par six assertions de plus, et prouve par **deux passes qui ne se recouvrent pas** (R3b 3/3 sur le remplissage, R7 6/6 sur le trait). Piege du **lambda GDScript capturant par valeur** re-rencontre (compteur de signaux) et ferme par un membre de classe. `MinimapProbe` **9 phases, 69 assertions, ALL GREEN 0 red**, **sept passes rouges** (3/3, 3/3, 3/3, 3/3, 8/7, 6/6) fichiers restaures byte-identiques. Regression rejouee sur **DEUX ARBRES** : `KartProbe` 150/1 **des deux cotes** (meme assertion, meme 977,0 -- pre-existant, pas CH46), `ReverseProbe` / `SailBoatProbe` 42/0 / `CoveProbe` 178/0 / `SledProbe` identiques | [CH46_MINIMAP.md](CH46_MINIMAP.md) | 1 | 492 | 7 sept 2026 | `staging` |
| CH47 | **LA LISIBILITE DE LA MINIMAP** -- retour device de Mathieu sur CH46 (« je ne comprends rien a cette map, il y a plein de points, on n'arrive pas a distinguer les amis de Keepy des vehicules »). ⚠️ **CH46 est sorti 69 VERT SUR UNE CARTE ILLISIBLE PARCE QU'AUCUNE DE SES ASSERTIONS NE COMPARAIT DEUX TYPES ENTRE EUX** : `vehicle` et `npc` partageaient la **meme cellule d'atlas** (disque r 3,9), seule la teinte les separait, et la teinte est le canal que `CLAUDE.md` documente comme non gatable. ⚠️ **LA RECON A TROUVE MIEUX QUE DE L'ENCOMBREMENT : SEPT DES NEUF MARQUEURS PNJ ETAIENT EPINGLES A L'ORIGINE DU MONDE**, sous le marqueur de Keepy -- `HubBoar/Cat/Fawn/Beaver` sont des **controleurs vides** et CH46 avait ecrit `mark(self)` au lieu de `mark(_critter)` ; prouve par un **temoin** (frames 24 -> 924, ticks 9 560 -> 158 637 ms, positions inchangees) parce que deux lectures byte-identiques passent gratuitement contre un monde qui n'a jamais tourne. **LEVIER A** : trois rangs de saillance, chaque type sa **forme ET sa taille** (disque 7,0 / losange 7,6 / triangle 3,0 / petit disque 2,7 ; portees **mesurees sur l'atlas cuit** 8,25 / 8,00 / 8,06 / 4,00 px), teintes remises **en ordre de luminance** (0,8392 / 0,6476 / 0,5007 / 0,3643 -- le bleu PNJ de CH46 etait PLUS CLAIR que son orange vehicule). **LEVIER B** : fusion des grappes de meme type, `merge_px` = **deux fois la portee mesuree** (0,00 / 16,00 / 16,12 / 8,00 px), regroupement **par MENEUR et non par liaison simple** (un enchainement poserait le glyphe a 30 px d'un membre), glyphe sur un membre reel jamais sur un barycentre, **Keepy ne fusionne jamais** (seuil nul, pas un `if`) ; **37 marqueurs -> 30 glyphes**, aucun marqueur retire. **LE CRITERE, ECRIT POUR LA PREMIERE FOIS** : separation **par COUVERTURE** contre une ligne de base ou le type est LEVE de la carte -- matrice complete des 6 paires 0,1024 / 0,2016 / 0,2976 / **0,0992** / 0,1952 / **0,0960**, plancher de bruit **0,0000** sur 28 boites, soit **960x le plancher**. **COUT : +62 prim / +1 CALL** (contre +76/+1 pour CH46) sur une reference reproduite **au chiffre pres** (71 609 / 311). ⚠️ **Le rayon rang 3 est 2,7 et pas 2,3 PARCE QUE LA SONDE A REFUSE 2,3** (quatre pixels de teinte au lieu de six) -- l'assertion avait raison sur l'art. ⚠️ **Et le rayon d'occlusion herite de CH46 etait la CELLULE et non l'ENCRE** : porte sur une cellule de 25 px, il a exclu **les quatorze** glyphes de lieux et la phase est sortie **0/0**, rattrapee par le garde `tried > 0` de CH46. `MinimapProbe` **11 phases, 125 assertions, ALL GREEN 0 red**, **cinq passes rouges toutes exactement conformes a leur prediction** (2/2, 6/6, 2/2, 1/1, 2/2), fichiers restaures byte-identiques. `MOUSE_FILTER_IGNORE` re-prouve (2 / 0 / 2). **NON EVALUE** : device, pluie et neige, cout CPU du `_draw` (regroupement quadratique), et les **trois oiseaux fantomes** (`visible = false` et pourtant marques a l'origine) -- les retirer serait retirer un marqueur, ce que la decision 1 interdit : question ouverte pour Mathieu | [CH47_MINIMAP_LISIBILITE.md](CH47_MINIMAP_LISIBILITE.md) | 1 | 390 | 7 sept 2026 | `staging` |
| CH48 | **DES MINIATURES REELLES SUR LA MINIMAP** -- second retour device de Mathieu, apres CH47 vert a 125 assertions : « je la trouve pas lisible ». 22 portraits (Keepy, 12 vehicules, 9 PNJ) cuits UNE fois depuis le hub CONSTRUIT ; les 15 lieux gardent la forme abstraite. ⚠️ **LA METHODE DEMANDEE N'A PAS PU REPONDRE** : la separation par COUVERTURE **empire quand l'icone grandit** (pire paire meme-rang 0,551 a 16 px contre 0,483 a 64 px -- a 16 px une plus grande part de la boite est du bord) ; un critere qui s'ameliore quand l'image retrecit ne peut pas nommer une taille minimale. Derive sur la **fidelite de reconstruction**, la seule courbe qui degrade : rendement par pixel de cellule 0,0118 -> 0,0032, **32 px est le dernier barreau rendant la moitie du premier**. Carte derivee du seuil de **CH46 lui-meme** (« 37 marqueurs couvrent 16 % ») : 155x280 -> 28,2 % d'encre, **211x380 -> 15,3 %**, premier barreau sous le seuil, **1,36x lineairement**. ⚠️ **LES COULEURS REELLES NE PASSENT PAS 3,0:1, ET LE BALAYAGE LE PROUVE** : de w=0,0 a w=1,0 (bandes lavees jusqu'au blanc pur), **la pire vignette ne depasse jamais 2,6 % de son encre au-dessus du plancher**. Un ratio de contraste est un ton contre un ton ; un blaireau a une fourrure blanche ET un masque noir. Le plancher est donc porte par un **PLATEAU SOMBRE** (L 0,0070, pire ratio **4,38:1** contre les huit bandes) et la vignette n'a plus qu'a franchir le plateau (47,2 % a 100 % de son encre). ⚠️ **ET LA DESATURATION N'EST PAS LE LEVIER DE CONTRASTE** : desaturer a clarte constante deplace `GRASS_A` de L 0,4717 a 0,4491. La demande porte sur la CHROMA ; un lavage vers le blanc laisse **(1-w)** de la chroma d'une bande. Fenetre **[0,3382 ; 0,6545]** (plancher : la bande la plus criarde ne doit plus crier plus fort que la chroma moyenne des 22 vignettes ; plafond : la paire separee par le ton la plus serree doit rester au-dessus du saignement de 8 %), le lot livre le **PLANCHER w=0,34**. La premisse « palette marecage L=0,1035 » du brief **ne tient pas pour ce hub** (bandes VOIE A mesurees L 0,1996 a 0,9320, toutes CLAIRES). ⚠️ **UNE SEULE ASSERTION DE BAKE A DEBUSQUE TROIS CAUSES** (17 rouges sur 22) : monde non fige, **`visibility_range_end` est un cull de DISTANCE** (16 rouges -> 6 a lui seul), et `haze`+`rim` sont deux vrais termes de distance du rendu livre -- contrat change pour la **distance propre de la camera du jeu (11,7034 u)**. ⚠️ **`get_aabb()` a cadre les deux rigs sur RIEN** (0,01 u a travers l'`Armature` Mixamo : couverture 1,0000 et 0,0000) -- le cadre est desormais **TROUVE EN RENDANT**. ⚠️ **Et la sonde s'est collisionnee elle-meme** : ours et blaireau tous deux etiquetes `World/Node3D`, la seconde prise ecrase la premiere, la matrice rapporte **0,0000** pour deux maillages de 5846 et 5623 triangles. ⚠️ **LE CRITERE DE CH47 NE VOIT PAS CES MARQUEURS** : |couverture(A)-couverture(B)| vaut ~0,02 pour chaque paire de portraits **par construction** et lirait **0,0000 pour deux portraits IDENTIQUES en appelant ca une reussite** ; remplace par une couverture de **DESACCORD** (0,5721 au pire, plancher 0,0000). **Fusion ramenee de DEUX portees a UNE** : a 32 px, « leurs encres se touchent » fait **19 unites monde** et pliait {HopBall, Balloon_0, bateau} en un seul portrait. **Placement sous-pixel** re-rencontre par l'autre bout (anneau a 0,835 de son ton, sonde lisant ZERO) -- glyphes cales sur le pixel entier. **Anneau de rang sorti de la cellule** vers trois cellules dessinees, parce que quatre entites rejoignent leur groupe APRES le bake et sortaient avec le lisere noir de repli. **Keepy dessine a 1,28x** : 184 px d'anneau contre 176, soit 4,5 %, n'est pas « au premier coup d'oeil ». **COUT : +48 prim / +1 CALL** (anneaux compris +80/+1), atlas runtime 256x700 pour 78 cellules, planche versionnee **25 698 octets**. `MinimapProbe` **188 assertions, ALL GREEN 0 red**, **cinq passes rouges** (21, 5, 2, 5, 3) fichiers restaures byte-identiques. ⚠️ **R8 A DEMASQUE UN FAUX VERT DANS CETTE SONDE MEME** : la phase de contraste lisait le pixel le plus sombre n'importe ou dans le glyphe et est sortie ALL GREEN sur un plateau eclairci -- un portrait a ses propres pixels sombres ; refaite en marchant le PERIMETRE (488 echantillons, pire 3,56:1, 0 sous le plancher). Deux autres defauts de sonde trouves par leurs propres chiffres : la pause inseree dans DEUX phases (six rouges, aucun au sujet de la carte) et un blind check PNJ qui ne pouvait pas discriminer (l'animal est ENFANT du controleur). **NON ETABLI** : device ; **char a voile et voilier sont la MEME image** (memes deux `.glb`, 0,0000 a toute taille -- gate comme un fait, non repare) ; pluie et neige ; cout CPU du `_draw` | [CH48_MINIMAP_VIGNETTES.md](CH48_MINIMAP_VIGNETTES.md) | 1 | 541 | 8 sept 2026 | `staging` |

## Archive

Chantiers **clos, sans objet ou historiques**. Déplacés intégralement,
**jamais condensés** : une approche abandonnée garde sa mesure, parce que
c'est la mesure qui explique pourquoi elle a été abandonnée — et c'est ce
qui évite de la refaire.

| Fichier | Sections | Lignes | Période | Contenu |
|---|---|---|---|---|
| [ARCHIVE/A01_MODE_SOMBRE_ET_F10.md](ARCHIVE/A01_MODE_SOMBRE_ET_F10.md) | 2 | 286 | 9 → 11 août 2026 | le mode sombre par inversion plein écran, **supprimé** par la refonte marécage ; et les deux décisions de teinte F10 qu'elle a rendues **sans objet** |
| [ARCHIVE/A02_CLASSEMENT_PWA_CLOS.md](ARCHIVE/A02_CLASSEMENT_PWA_CLOS.md) | 2 | 235 | 15 → 16 août 2026 | l'enquête `accept_gzip` sur le classement en PWA — **close**, validée device des deux côtés, diagnostic retiré |
| [ARCHIVE/A03_INCIDENTS_INFRA_RESOLUS.md](ARCHIVE/A03_INCIDENTS_INFRA_RESOLUS.md) | 2 | 174 | 8 août 2026 | `vercel alias set` « Not able to load user » (scope de token), et le blocage GitHub Actions transitoire |
| [ARCHIVE/A04_AUTH_IMPASSES.md](ARCHIVE/A04_AUTH_IMPASSES.md) | 2 | 288 | 17 août 2026 | `signInWithRedirect` puis `signInWithPopup` — les **deux impasses** Safari iOS, avec leurs mesures, avant le proxy `/__/auth/*` |
| [ARCHIVE/A05_RECONS_SANS_SUITE.md](ARCHIVE/A05_RECONS_SANS_SUITE.md) | 8 | 1768 | 25 → 27 août 2026 | recons pures n'ayant produit **aucun code**, et lots **arrêtés en recon** sur un seuil franchi (lot D plateau 35, water-walk, spawn-lake, waterline, lake-move) |

**Total archive : 16 sections, 2 751 lignes.**

## Comptabilité de non-perte (LOT H)

| | lignes |
|---|---|
| `CLAUDE.md` d'origine (hors titre + ligne vide) | **26 321** |
| dont section de doctrine conservée dans `CLAUDE.md` | 56 |
| dont réparties dans les 20 fichiers de chantier | 23 544 |
| dont réparties dans les 5 fichiers d'archive | 2 721 |
| **somme** | **26 321** ✅ |

Vérifié **byte à byte** : les 133 sections ont été réassemblées dans leur
ordre d'origine depuis les fichiers générés et comparées au fichier source —
identiques, ordre compris. Aucune section n'apparaît deux fois, aucune ne
manque.

Le total actuel des fichiers de `docs/lots/` (**26 581** lignes) dépasse les
26 321 d'origine de **260 lignes**, et le compte tombe juste à la ligne près :

| | lignes |
|---|---|
| contenu verbatim déplacé dans `docs/lots/` | +26 265 |
| 25 en-têtes de six lignes (un par fichier) | +150 |
| section LOT A écrite par le LOT H | +71 |
| section LOT F écrite par le LOT H | +94 |
| ligne vide de jointure au splice de `CH20_OURS.md` | +1 |
| **total** | **26 581** ✅ |

Les 56 lignes restantes des 26 321 d'origine sont la section de doctrine,
**conservée dans `CLAUDE.md`** et donc absente de `docs/lots/` :
26 265 + 56 = 26 321.
